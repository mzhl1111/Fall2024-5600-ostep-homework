# Use the VS Code Dev Containers base image
FROM mcr.microsoft.com/vscode/devcontainers/base:ubuntu-20.04

# Install additional packages if needed
RUN apt-get update && apt-get install -y \
    build-essential \
    && rm -rf /var/lib/apt/lists/*
